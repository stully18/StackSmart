# First Time Setup

If you're on Ubuntu/Debian and Python venv creation fails, install the required package first:

```bash
sudo apt install python3.12-venv
```

Then run the app:

```bash
cd net-worth-optimizer
./start.sh
```

See [QUICKSTART.md](QUICKSTART.md) for more details.
